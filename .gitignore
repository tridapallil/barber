node_modules
.next
out
.DS_Store
npm-debug.log*
.env
.env*.local

# Dados antigos em arquivo (antes do MongoDB). Mantidos fora do Git por conterem
# clientes reais e hash de senha; use `npm run migrar` para levá-los ao banco.
/data/
