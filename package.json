{
  "name": "salao",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p ${PORT:-3210}",
    "build": "next build",
    "start": "next start -p ${PORT:-3210}",
    "instalar": "npm ci",
    "producao": "npm ci && npm run build && npm start",
    "migrar": "node scripts/migrar-mongo.js",
    "exemplo": "node scripts/dados.js exemplo",
    "limpar": "node scripts/dados.js limpar"
  },
  "dependencies": {
    "mongodb": "^6.21.0",
    "next": "^15.5.25",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "engines": {
    "node": ">=18.18"
  }
}
